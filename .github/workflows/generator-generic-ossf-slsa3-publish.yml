# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Full-Width Paragraph</title>
    <style>
        body { font-family: Arial, sans-serif; text-align: justify; background-color: #f4f4f4; padding: 20px; }
        .login-container, .content { max-width: 400px; margin: auto; background: white; padding: 20px; box-shadow: 0px 0px 10px rgba(0,0,0,0.1); border-radius: 8px; }
        .content { display: none; }
        input { width: 100%; padding: 10px; margin: 10px 0; border: 3px solid #ccc; border-radius: 5px; }

        /* Coffee-Themed Button */
        button {
            background: #6F4E37; /* Coffee Brown */
            color: white;
            padding: 12px;
            border: none;
            width: 100%;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            font-weight: bold;
            transition: 0.3s;
        }

        /* Hover effect */
        button:hover {
            background: #4B3621; /* Darker Coffee */
        }

        /* Sidebar Links */
        #sidebar a {
            display: block;
            padding: 10px;
            background: #6F4E37;
            color: white;
            text-decoration: none;
            margin: 5px 0;
            text-align: center;
            border-radius: 5px;
            transition: 0.3s;
        }

        #sidebar a:hover {
            background: #4B3621;
        }
    </style>
    <script>
        // Prevent Right-Click and Download
        document.addEventListener('contextmenu', event => event.preventDefault());
        document.addEventListener('keydown', function(event) {
            if (event.ctrlKey && (event.key === 's' || event.key === 'p')) {
                event.preventDefault();
            }
        });

        // Simple Login System
        function login() {
            let user = document.getElementById("username").value;
            let pass = document.getElementById("password").value;
            if (user === "admin" && pass === "1234") { // Replace with actual authentication method
                document.querySelector(".login-container").style.display = "none";
                document.querySelector(".content").style.display = "block";
            } else {
                alert("Invalid username or password!");
            }
        }

        // Show the selected section
        function showSection(sectionId) {
            document.querySelectorAll('.section').forEach(section => {
                section.style.display = 'none';
            });
            let selectedSection = document.getElementById(sectionId);
            if (selectedSection) {
                selectedSection.style.display = 'block';
            }
        }

        document.addEventListener("DOMContentLoaded", function () {
            showSection('agr111');
        });
    </script>
</head>
<body>

    <!-- Login Portal -->
    <div class="login-container">
        <h2>Log in</h2>
        <input type="text" id="username" placeholder="Enter Username">
        <input type="password" id="password" placeholder="Enter Password">
        <button onclick="login()">Login</button>
    </div>

    <!-- Marquee Header -->
    <marquee onmouseover="this.stop();" onmouseout="this.start();">ALL YEAR ONE AGRICULTURE MODULES</marquee>

    <!-- App Content (Hidden Until Login) -->
    <div class="content" style="display: none;">
        <div class="brand-bar">
            <h2>ALL YEAR 1 AGRICULTURE</h2>
        </div>

        <!-- Sidebar Navigation -->
        <div id="sidebar">
            <h4>AGRICULTURE YEAR1</h4>
            <a href="#" onclick="showSection('agr111')">AGR111</a>
            <a href="#" onclick="showSection('agr112')">AGR112</a>
            <a href="#" onclick="showSection('agr121')">AGR121</a>
            <a href="#" onclick="showSection('agr122')">AGR122</a>
        </div>

        <!-- Content Sections -->
        <div id="AGR111" class="section">
            <h2>AGR111 Notes</h2>
            <p>INTRO TO SOCIOLOGY OF EDUCATION</p>
        </div>

        <div id="AGR112" class="section" style="display: none;">
            <h2>AGR112 Notes</h2>
            <p>CURRICULUM THEORY COURSE MANUAL.</p>
        </div>

        <div id="AGR121" class="section" style="display: none;">
            <h2>AGR121 Notes</h2>
            <p>AGR121 INSTRUCTIONAL MEDIA.</p>
        </div>

        <div id="AGR122" class="section" style="display: none;">
            <h2>AGR122 Notes</h2>
            <p>Its yo boy Bule WORKING ON the app.</p>
        </div>
    </div>

</body>
</html>


# This workflow lets you generate SLSA provenance file for your project.
# The generation satisfies level 3 for the provenance requirements - see https://slsa.dev/spec/v0.1/requirements
# The project is an initiative of the OpenSSF (openssf.org) and is developed at
# https://github.com/slsa-framework/slsa-github-generator.
# The provenance file can be verified using https://github.com/slsa-framework/slsa-verifier.
# For more information about SLSA and how it improves the supply-chain, visit slsa.dev.

name: SLSA generic generator
on:
  workflow_dispatch:
  release:
    types: [created]

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      digests: ${{ steps.hash.outputs.digests }}

    steps:
      - uses: actions/checkout@v4

      # ========================================================
      #
      # Step 1: Build your artifacts.
      #
      # ========================================================
      - name: Build artifacts
        run: |
            # These are some amazing artifacts.
            echo "artifact1" > artifact1
            echo "artifact2" > artifact2

      # ========================================================
      #
      # Step 2: Add a step to generate the provenance subjects
      #         as shown below. Update the sha256 sum arguments
      #         to include all binaries that you generate
      #         provenance for.
      #
      # ========================================================
      - name: Generate subject for provenance
        id: hash
        run: |
          set -euo pipefail

          # List the artifacts the provenance will refer to.
          files=$(ls artifact*)
          # Generate the subjects (base64 encoded).
          echo "hashes=$(sha256sum $files | base64 -w0)" >> "${GITHUB_OUTPUT}"

  provenance:
    needs: [build]
    permissions:
      actions: read   # To read the workflow path.
      id-token: write # To sign the provenance.
      contents: write # To add assets to a release.
    uses: slsa-framework/slsa-github-generator/.github/workflows/generator_generic_slsa3.yml@v1.4.0
    with:
      base64-subjects: "${{ needs.build.outputs.digests }}"
      upload-assets: true # Optional: Upload to a new release
